---
phase: 06-hardening-launch
plan: 05
type: execute
wave: 4
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
  - "06-04"
files_modified:
  - docs/PERFORMANCE.md
autonomous: false
requirements:
  - REQ-HARD-13
  - REQ-HARD-14

must_haves:
  truths:
    - "All 63+ backend tests pass with 80% coverage after all phase changes"
    - "Zero TypeScript errors in the frontend after all phase changes"
    - "k6 smoke test result is documented with actual p95 latency value"
    - "All TODO/FIXME/HACK occurrences in source are catalogued for human review"
    - "PERFORMANCE.md exists with load test results and tech debt inventory"
  artifacts:
    - path: "docs/PERFORMANCE.md"
      provides: "Load test results + tech debt inventory"
  key_links:
    - from: "load-tests/smoke.js"
      to: "docs/PERFORMANCE.md"
      via: "k6 output captured and pasted into PERFORMANCE.md"
      pattern: "p\\(95\\)"
---

<objective>
Run the full test suite, load test, and codebase audit to confirm the platform is production-ready and document the results.

Purpose: Final gate before declaring Phase 6 complete. All quality signals must be green or acknowledged.
Output: docs/PERFORMANCE.md with load test results, coverage, and tech debt list.
</objective>

<execution_context>
@G:/Users/fadya/.claude/get-shit-done/workflows/execute-plan.md
@G:/Users/fadya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@G:/AI/AXIOM-V2/.planning/ROADMAP.md
@G:/AI/AXIOM-V2/backend/tests/conftest.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full regression + coverage gate + tech debt scan</name>
  <files>
    docs/PERFORMANCE.md
  </files>
  <action>
1. Run the full backend test suite with coverage:
   ```bash
   cd "G:/AI/AXIOM-V2/backend"
   python -m pytest tests/ -v --cov=app --cov-report=term-missing --cov-fail-under=80 2>&1
   ```
   Capture the output. If coverage is below 80%, identify the least-covered modules and note them in PERFORMANCE.md — do NOT fail the task, document the gap.

2. Run the TypeScript check:
   ```bash
   cd "G:/AI/AXIOM-V2/frontend"
   npx tsc --noEmit 2>&1
   ```
   Must return zero errors. If errors exist, fix them before proceeding.

3. Scan for tech debt markers:
   ```bash
   grep -rn "TODO\|FIXME\|HACK\|XXX\|TEMP\|WORKAROUND" \
     "G:/AI/AXIOM-V2/backend/app/" \
     "G:/AI/AXIOM-V2/frontend/src/" \
     --include="*.py" --include="*.ts" --include="*.tsx" 2>/dev/null
   ```
   Capture all results.

4. Create docs/PERFORMANCE.md with the following structure:

```markdown
# AXIOM V2 — Performance & Quality Report
Generated: {current date}

## Backend Test Coverage
- Total tests: {N}
- Passed: {N}
- Coverage: {N}%
- Coverage gate (80%): {PASS/FAIL}

### Least-covered modules (if any)
| Module | Coverage |
|--------|----------|
| ...    | ...      |

## Frontend Type Safety
- TypeScript errors: {0 or list}

## Tech Debt Inventory
Items found via grep for TODO/FIXME/HACK:

| File | Line | Type | Comment |
|------|------|------|---------|
{rows from grep output, or "None found"}

## Load Test Results
{Populated in Task 2 below}

## Next Actions
{Any items requiring follow-up}
```
  </action>
  <verify>
    <automated>cd "G:/AI/AXIOM-V2/backend" && python -m pytest tests/ --co -q 2>&1 | tail -5</automated>
  </verify>
  <done>docs/PERFORMANCE.md exists with actual test count, coverage percentage, TypeScript error count (must be 0), and tech debt table. Backend tests all pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full regression test run, TypeScript check, and tech debt scan. docs/PERFORMANCE.md skeleton created.</what-built>
  <how-to-verify>
    1. Run the k6 load test (requires k6 installed: https://k6.io/docs/getting-started/installation/):
       ```bash
       # Start the backend first
       cd "G:/AI/AXIOM-V2/backend"
       uvicorn app.main:app --port 8000 &

       # Run k6
       cd "G:/AI/AXIOM-V2"
       k6 run load-tests/smoke.js
       ```
       The test will run 100 VUs ramping over 30 seconds hitting GET /api/listings.

    2. Copy the k6 summary output (especially the http_req_duration p95 line) and paste it into the "Load Test Results" section of docs/PERFORMANCE.md.

    3. Confirm the threshold result:
       - If k6 exits 0: p95 < 500ms — PASS
       - If k6 exits non-zero: p95 >= 500ms — review PERFORMANCE.md and decide if acceptable for pre-production

    4. Review the tech debt table in PERFORMANCE.md:
       - Mark any items that need immediate attention before launch
       - Mark rest as "deferred"

    5. Final check — confirm all 5 green signals:
       - [ ] Backend tests: all pass with coverage >= 80%
       - [ ] TypeScript: zero errors
       - [ ] k6: p95 documented (target < 500ms)
       - [ ] PERFORMANCE.md exists and complete
       - [ ] No high/critical CVEs in backend/SECURITY.md or frontend/SECURITY.md

  </how-to-verify>
  <resume-signal>Type "approved" when k6 results are pasted into PERFORMANCE.md and all 5 signals reviewed. Describe any p95 result above 500ms or CVEs requiring immediate action.</resume-signal>
</task>

</tasks>

<verification>
- `cat docs/PERFORMANCE.md` — file exists, contains test count, coverage %, tech debt table, and load test results
- `cd frontend && npx tsc --noEmit` — zero errors
- `cd backend && python -m pytest tests/ -v` — all tests pass
</verification>

<success_criteria>
- docs/PERFORMANCE.md exists with load test p95 value, test coverage %, and tech debt inventory
- All 63+ backend tests pass
- Zero TypeScript errors in frontend
- k6 p95 result documented (whether pass or fail — documented is required)
- Phase 6 can be marked complete in ROADMAP.md
</success_criteria>

<output>
After completion:
1. Create `.planning/phases/06-hardening-launch/06-05-SUMMARY.md` with final test counts, coverage %, k6 p95 result, and CVE summary.
2. Update `.planning/ROADMAP.md` — mark Phase 6 as complete with today's date.
3. Update `.planning/STATE.md` — set active phase to None, last completed to Phase 6.
</output>
