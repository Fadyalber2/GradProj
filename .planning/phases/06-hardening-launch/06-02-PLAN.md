---
phase: 06-hardening-launch
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - .github/workflows/backend-ci.yml
  - .github/workflows/frontend-ci.yml
  - backend/railway.json
  - docs/DEPLOYMENT.md
autonomous: true
requirements:
  - REQ-HARD-05
  - REQ-HARD-06
  - REQ-HARD-07
  - REQ-HARD-08

must_haves:
  truths:
    - "Pushing to main triggers both backend and frontend CI workflows"
    - "Backend CI runs ruff, mypy, pytest with 80% coverage gate, and pip-audit in sequence"
    - "Frontend CI runs eslint, tsc --noEmit, npm run build, and npm audit in sequence"
    - "Railway can deploy the backend using railway.json with $PORT variable expansion"
    - "DEPLOYMENT.md documents all env vars needed for Vercel and Railway deployments"
  artifacts:
    - path: ".github/workflows/backend-ci.yml"
      provides: "GitHub Actions backend pipeline"
    - path: ".github/workflows/frontend-ci.yml"
      provides: "GitHub Actions frontend pipeline"
    - path: "backend/railway.json"
      provides: "Railway deployment config"
    - path: "docs/DEPLOYMENT.md"
      provides: "Deployment runbook"
  key_links:
    - from: ".github/workflows/backend-ci.yml"
      to: "backend/requirements.txt"
      via: "pip install -r requirements.txt with pip cache"
      pattern: "pip.*cache"
    - from: ".github/workflows/frontend-ci.yml"
      to: "frontend/package.json"
      via: "npm ci with node_modules cache"
      pattern: "npm.*cache|actions/cache"
---

<objective>
Establish automated CI/CD pipelines and deployment configuration so every PR is validated before merge and the backend can deploy to Railway.

Purpose: Catch regressions before they reach main. Provide deployment runbook for Vercel and Railway.
Output: Two GitHub Actions workflows, railway.json, DEPLOYMENT.md.
</objective>

<execution_context>
@G:/Users/fadya/.claude/get-shit-done/workflows/execute-plan.md
@G:/Users/fadya/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@G:/AI/AXIOM-V2/.planning/ROADMAP.md
@G:/AI/AXIOM-V2/backend/requirements.txt
@G:/AI/AXIOM-V2/frontend/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: GitHub Actions CI workflows</name>
  <files>
    .github/workflows/backend-ci.yml,
    .github/workflows/frontend-ci.yml
  </files>
  <action>
1. Create .github/workflows/backend-ci.yml:
   ```yaml
   name: Backend CI
   on:
     push:
       branches: [main]
     pull_request:
       branches: [main]
   jobs:
     test:
       runs-on: ubuntu-latest
       defaults:
         run:
           working-directory: backend
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-python@v5
           with:
             python-version: "3.12"
             cache: "pip"
             cache-dependency-path: backend/requirements.txt
         - name: Install dependencies
           run: pip install -r requirements.txt ruff mypy pytest pytest-cov pip-audit
         - name: Lint (ruff)
           run: ruff check app/
         - name: Type check (mypy)
           run: mypy app/ --ignore-missing-imports --no-strict-optional
         - name: Test with coverage
           run: pytest tests/ -v --cov=app --cov-fail-under=80
           env:
             SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
             SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
             JWT_SECRET: ${{ secrets.JWT_SECRET }}
             ALLOWED_ORIGINS: "http://localhost:3000"
             REDIS_URL: ""
         - name: Security audit (pip-audit)
           run: pip-audit -r requirements.txt
   ```

2. Create .github/workflows/frontend-ci.yml:
   ```yaml
   name: Frontend CI
   on:
     push:
       branches: [main]
     pull_request:
       branches: [main]
   jobs:
     build:
       runs-on: ubuntu-latest
       defaults:
         run:
           working-directory: frontend
       steps:
         - uses: actions/checkout@v4
         - uses: actions/setup-node@v4
           with:
             node-version: "20"
             cache: "npm"
             cache-dependency-path: frontend/package-lock.json
         - name: Install dependencies
           run: npm ci
         - name: Lint (ESLint)
           run: npx eslint src/ --max-warnings 0
         - name: Type check
           run: npx tsc --noEmit
         - name: Build
           run: npm run build
           env:
             NEXT_PUBLIC_API_URL: "http://localhost:8000"
             NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
             NEXT_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_ANON_KEY }}
         - name: Security audit
           run: npm audit --audit-level=high
   ```

Note: Both workflows use separate jobs (not matrix) for parallel execution. The frontend build step needs NEXT_PUBLIC_* env vars as CI secrets to avoid build failures on missing env values.
  </action>
  <verify>
    <automated>ls "G:/AI/AXIOM-V2/.github/workflows/" && echo "workflows exist"</automated>
  </verify>
  <done>Both .yml files exist under .github/workflows/. YAML syntax is valid (no tabs, correct indentation). pip cache and npm ci cache configured.</done>
</task>

<task type="auto">
  <name>Task 2: Railway config + deployment documentation</name>
  <files>
    backend/railway.json,
    docs/DEPLOYMENT.md
  </files>
  <action>
1. Create backend/railway.json:
   ```json
   {
     "$schema": "https://railway.app/railway.schema.json",
     "build": {
       "builder": "NIXPACKS"
     },
     "deploy": {
       "startCommand": "uvicorn app.main:app --host 0.0.0.0 --port $PORT",
       "healthcheckPath": "/api/health",
       "healthcheckTimeout": 30,
       "restartPolicyType": "ON_FAILURE",
       "restartPolicyMaxRetries": 3
     }
   }
   ```
   CRITICAL: startCommand must be a string (shell form), NOT a JSON array — Railway expands $PORT in shell form only.

2. Create docs/DEPLOYMENT.md with the following sections:

   ## Vercel (Frontend)
   - No vercel.json needed — Next.js 16 auto-detected
   - Connect GitHub repo in Vercel dashboard → select `frontend/` as root directory
   - Required environment variables in Vercel project settings:
     | Variable | Source |
     |---|---|
     | NEXT_PUBLIC_SUPABASE_URL | Supabase dashboard → Settings → API |
     | NEXT_PUBLIC_SUPABASE_ANON_KEY | Supabase dashboard → Settings → API |
     | NEXT_PUBLIC_API_URL | Your Railway backend URL (e.g. https://axiom-v2.railway.app) |

   ## Railway (Backend)
   - Connect GitHub repo in Railway dashboard → select `backend/` as root directory
   - railway.json is auto-detected
   - Required environment variables in Railway project settings:
     | Variable | Source |
     |---|---|
     | SUPABASE_URL | Supabase dashboard → Settings → API |
     | SUPABASE_SERVICE_ROLE_KEY | Supabase dashboard → Settings → API (keep secret) |
     | JWT_SECRET | Supabase dashboard → Settings → JWT Settings |
     | ALLOWED_ORIGINS | Comma-separated: `https://your-app.vercel.app,https://axiom.eg` |
     | REDIS_URL | Railway Redis plugin → connect string (optional, omit to use in-memory) |
     | TWILIO_ACCOUNT_SID | Twilio console |
     | TWILIO_AUTH_TOKEN | Twilio console |
     | TWILIO_PHONE_NUMBER | Twilio console |

   ## Branch Protection (Manual Step — GitHub UI)
   1. Go to repo Settings → Branches → Add rule for `main`
   2. Enable: "Require status checks to pass before merging"
   3. Select: "Backend CI / test" and "Frontend CI / build"
   4. Enable: "Require at least 1 approving review"
   5. Enable: "Dismiss stale reviews when new commits are pushed"

   ## Health Check
   After Railway deploy: `curl https://your-backend.railway.app/api/health` → `{"status":"ok","version":"2.0.0"}`
  </action>
  <verify>
    <automated>python -c "import json; d=json.load(open('G:/AI/AXIOM-V2/backend/railway.json')); assert isinstance(d['deploy']['startCommand'], str), 'startCommand must be string'; print('railway.json valid')"</automated>
  </verify>
  <done>backend/railway.json exists with string startCommand containing $PORT. docs/DEPLOYMENT.md exists with Vercel env vars, Railway env vars, and branch protection steps.</done>
</task>

</tasks>

<verification>
- Both workflow files exist and have valid YAML structure
- `python -c "import json; json.load(open('backend/railway.json'))"` — valid JSON
- railway.json startCommand is a string (not array) containing `$PORT`
- docs/DEPLOYMENT.md lists all required env vars for both platforms
</verification>

<success_criteria>
- Push to any branch with a PR against main triggers both CI workflows
- Backend CI: ruff → mypy → pytest (80% coverage gate) → pip-audit
- Frontend CI: eslint → tsc --noEmit → npm run build → npm audit
- Railway health check path is /api/health
- DEPLOYMENT.md documents every env var needed for both platforms
</success_criteria>

<output>
After completion, create `.planning/phases/06-hardening-launch/06-02-SUMMARY.md` with workflow structure and deployment config details.
</output>
